name: Deploy Next.js static site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - run: npm ci
        if: ${{ hashFiles('package-lock.json') != '' }}

      - run: npm install
        if: ${{ hashFiles('package-lock.json') == '' }}

      - name: Fetch official TRIPO images
        run: node fetch-tripo-assets.mjs

      - name: Verify downloaded images
        run: |
          test -s public/tripo/studio-gallery/gallery-01.webp
          test -s public/tripo/studio-gallery/gallery-02.webp
          test -s public/tripo/studio-gallery/gallery-06.webp
          test -s public/tripo/studio-gallery/gallery-07.webp
          test -s public/tripo/studio-gallery/case-jewelry.webp
          test -s public/tripo/studio-gallery/case-sculpture.webp
          test -s public/tripo/studio-gallery/case-miniature.webp
          test -s public/tripo/site-assets/service-core.webp
          test -s public/tripo/site-assets/service-input.webp
          test -s public/tripo/site-assets/service-print.webp
          test -s public/tripo/site-assets/service-topology.webp
          test -s public/tripo/site-assets/service-detail.webp
          ls -lh public/tripo/studio-gallery
          ls -lh public/tripo/site-assets

      - run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: /tripo-3d-printing-demo

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
